import {StyleSheet} from 'react-native';

export const Styles = StyleSheet.create({
  input: {
    height: 40,
    margin: 12,
    borderBottomWidth: 1,
    borderBottomColor: 'grey',
    padding: 10,
  },
});
