import React, { ReactElement } from 'react';

export interface IProfileProps {

}

const Profile: React.FC<IProfileProps> = ({ ... props }): ReactElement => {
  
  return (
    <>Profile</>
  )
}

export default (Profile);
