enum ROUTES {
  LOGIN = '/login',
  PAYMENT = '/payment',
  HOME = '/',
  MATCHES = '/matches',
  QUALIFYINGS = '/qualifyings',
  RANKING = '/ranking',
  TERMS = '/terms',
  WILDCARD = '*',
}

export {ROUTES}